{% if recipe.Photo -%}
<link rel="stylesheet" href="/assets/css/lightbox.css">
<div class ="image-gallery">
{% for name in recipe.Photo %}
    <div class="box">
    <a href="/assets/images/recipes/{{ name }}">
      <img src="/assets/images/recipes/{{ name }} " alt=""  class="img-gallery" />
     </a>
    </div>
 {% endfor %}
</div>
{% endif -%}